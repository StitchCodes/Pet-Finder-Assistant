import { Container } from "semantic-ui-react";

const Cancel = () => {
  return (
    <div>
      <Container
        textAlign="center"
        style={{ marginTop: "8em", marginBottom: "45rem" }}
      >
        <h1>Cancel</h1>
        <h2>Your payment was canceled.</h2>
      </Container>
    </div>
  );
};

export default Cancel;
